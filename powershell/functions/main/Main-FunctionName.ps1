<#
Main function that performs the core task of the composite GitHub action.  Any other functions needed can be placed in the helper functions directory.

If applicable, Use -InformationAction Continue when calling this function from action.yml so that output can easily be suppressed in Pester testing, leaving only the
output object returned at the end, which helps with automated testing.
#>
